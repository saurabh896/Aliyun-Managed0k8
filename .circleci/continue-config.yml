version: '2.1'

parameters:
  pathtodev:
    type: boolean
    default: false


workflows:
  build-code-workflow:
    when: << pipeline.parameters.pathtodev >>
    jobs:
      build:
        docker:
          - image: circleci/python:latest
        steps:
          - checkout
          - run: |
                command: ls